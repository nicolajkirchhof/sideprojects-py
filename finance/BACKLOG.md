# Apps — Swing Plot

## Completed

- ~~B-01 — Force re-fetch of today's bar on manual refresh~~ — shipped (skip_threshold logic + cursor clamping in `ibkr.py::daily_w_volatility`)
- ~~B-02 — Move Refresh to far-right with icon~~ — shipped (addStretch + ⟳ icon in `_app.py::_build_toolbar`)
- ~~B-03 — Use icon for Load button~~ — shipped (▶ icon + tooltip in `_app.py::_build_toolbar`)
